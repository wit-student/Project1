<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cover Letter Generator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/home"style="color: rgb(10, 105, 53);"><i class="fa-brands fa-google-play"></i>Riso</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
              <a class="nav-link active" aria-current="page" href="/home">Home</a>
              <a class="nav-link active " href="/generate">Ask Anything</a>
              <a  class="nav-link active" href="/previous" >Previous</a>  
            </div>
             <div class="navbar-nav ms-auto">
             
              <a class="nav-link active " href="/login">Login</a>
              <a class="nav-link active " href="/signup">SignUp</a>
     
           </div>
         
          </div>
        </div>
      </nav>
<div class="container">
    <br>
    <h2>AI Refferal Message Generator</h2>


    <form action="/generator-refferal-message" method="POST">
        <label  class="form-label">Company Name:</label>
        <input type="text" name="company"class="form-control" required><br>

        <label  class="form-label">Job Title:</label>
        <input type="text" name="jobTitle"class="form-control" required><br>

        <label  class="form-label">Your Skills:</label>
        <input type="text" name="skills" class="form-control"><br>
<label for="" class="form-label"> Experience:</label>
<textarea name="experience" class="form-control"></textarea>
<br>
        <button type="submit" class="btn btn-success">Generate Refferal Message</button>
    </form>

    <hr>

    <% if (RefferalMsg) { %>
        <h3>Your AI-Generated Refferal Message:</h3>
            <h2>Generated Text </h2>
            <p id="RefferalMsg"><%-RefferalMsg.replace(/\n/g, "<br>").replace(/\*\*(.*?)\*\*/g, "<b>$1</b>") %></p>
     
        <button  class="btn btn-success " id="downloadPdf"  >Download as PDF</button>
    <% } %>

<script>

    document.getElementById("downloadPdf")?.addEventListener("click", function () {
        const { jsPDF } = window.jspdf;
        const doc = new jsPDF();
        const coverLetterElement = document.getElementById("RefferalMsg");

        if (coverLetterElement) {
            const text = coverLetterElement.innerText; // Get full AI response
            const pageWidth = doc.internal.pageSize.getWidth() - 20; // Page width with margins
            const marginTop = 10; // Top margin
            const lineHeight = 10; // Space between lines
            const startX = 10; // Left margin
            let startY = marginTop; // Start position on Y-axis

            const splitText = doc.splitTextToSize(text, pageWidth); // Wrap text
            
            splitText.forEach((line, index) => {
                if (startY + lineHeight > doc.internal.pageSize.getHeight() - 10) {
                    doc.addPage(); // Add new page when text overflows
                    startY = marginTop; // Reset Y position
                }
                doc.text(line, startX, startY);
                startY += lineHeight; // Move to next line
            });

            doc.save("RefferalMsg.pdf");
        } else {
            alert("Refferal Message content not found!");
        }
    });
    
</script>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js"></script>

</body>
</html>